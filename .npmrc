# Puppeteer is only used for optional screenshot tooling. Skip its large Chrome
# download so `npm install` never fails on networks that block/corrupt it.
puppeteer_skip_download=true
PUPPETEER_SKIP_DOWNLOAD=true
