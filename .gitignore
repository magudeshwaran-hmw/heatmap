# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment — contains real DB credentials, never commit
.env

# Excel data files — large binary, not needed in repo
*.xlsx
*.xls

# Resume / personal PDFs — contain personal data, never commit
*.pdf
*.pdf.gz
# Resume Vault storage — gzipped employee resumes (PII), runtime data, never commit
storage/

# Duplicate subfolder (development artifact)
zenlap/

# Dev artifacts & AI-tooling caches
scratch_screenshots/
.antigravitycli/
.claude/
.env.local
ngrok.yml
